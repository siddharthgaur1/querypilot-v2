fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9   # UploadFile support
psycopg2-binary>=2.9
structlog>=24.1
python-dotenv>=1.0

# RAG. Pinned exactly to the chromadb/chroma server image tag in
# docker-compose.yml — the client/server wire protocol isn't stable across
# major versions (client 1.x speaks a v2 API the 0.5.20 server doesn't have).
chromadb==0.5.20
sentence-transformers>=3.0

# LLM backends (both optional — see querypilot_v2/core/llm.py; without either,
# demo mode serves pre-verified example queries from data/examples.json)
anthropic>=0.34
ollama>=0.3
